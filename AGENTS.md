# Bid Agent 项目开发规则

## 1. 工作目录与权限边界

唯一允许访问和修改的本地项目目录：

/Users/ylg/Desktop/work/北京/大岳咨询/bid-agent

禁止读取、扫描、修改该目录以外的文件。

服务器项目目录：

/opt/bid-agent

服务器地址：

101.200.154.141

服务器用户：

root

## 2. 开发与运行环境

本地 Mac：
- 负责 Codex、代码编辑、Git 操作
- 当前本地不保证具备完整 Docker、PostgreSQL、Redis 和生产运行环境
- 不得假设所有后端依赖已经在 Mac 本地运行

阿里云 ECS：
- Docker
- Docker Compose
- PostgreSQL
- pgvector
- Redis
- FastAPI
- LangGraph
- 项目正式运行和集成测试环境

服务器当前不保证安装 Codex CLI。
不要要求服务器依赖 Codex CLI 才能运行项目。

## 3. 默认工作流程

Codex 默认在 Mac 项目目录修改代码。

完成修改后：

1. 先检查 git diff
2. 运行本地可执行的静态检查或单元测试
3. 明确说明哪些测试因为本地依赖缺失未运行
4. 用户确认后再提交 Git
5. 通过 GitHub 同步到 ECS
6. 在 ECS 的 /opt/bid-agent 下运行 Docker 集成测试

不得把“代码写完”视为“功能完成”。

功能完成必须包括：
- 代码实现
- 配置检查
- 数据库迁移检查
- Docker 环境检查
- 接口测试
- 错误处理检查

## 4. 执行前置检查

每次实现新功能前，必须先检查：

- 当前目录
- git status
- 相关现有代码
- pyproject.toml 或依赖文件
- docker-compose.yml
- 环境变量模板
- 数据库模型和迁移
- 现有测试

不要直接凭假设新建重复模块。

## 5. 修改原则

- 先输出实施计划，再修改代码
- 小步修改，不进行未经确认的大规模重构
- 优先复用现有结构
- 不删除现有功能
- 不修改项目目录之外的配置
- 不读取 SSH 密钥、个人文件或其他项目
- 不把密钥、密码和 Token 写入代码或 Git
- 所有新增环境变量必须同步更新 .env.example
- 所有新增依赖必须写入 pyproject.toml
- 所有数据库结构修改必须提供迁移方案

## 6. 当前产品范围

V1 只重点完成技术方案生成闭环：

上传招标文件
→ 文档解析
→ 提取技术要求与评分点
→ 生成技术方案目录
→ 分章节生成
→ 自动校核
→ 导出 Word

优先级：

1. Requirement Agent
2. Outline Planner
3. Section Writer
4. Reviewer
5. DOCX 导出
6. PGVector / RAG 增强

不要优先扩展与当前闭环无关的功能。

## 7. 生成质量要求

- Requirement 必须可追溯到原文和页码
- 每个章节必须记录响应哪些 Requirement
- 不允许虚构案例、资质、参数和产品能力
- 不一次生成整本方案
- 按章节生成和审核
- 中间结果必须持久化
- 支持失败重试和断点继续

## 8. 每次任务的输出格式

开始编码前先输出：

1. 对需求的理解
2. 涉及的已有文件
3. 前置条件
4. 实施步骤
5. 风险
6. 测试方式

编码完成后输出：

1. 修改了哪些文件
2. 完成了什么
3. 尚未完成什么
4. 本地运行了哪些测试
5. 哪些测试必须在 ECS 上执行
6. 部署命令
7. 回滚方式
