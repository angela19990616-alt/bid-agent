# Bid Agent 项目开发规则

## 1. 工作目录与权限边界

唯一允许访问和修改的本地项目目录：

/Users/ylg/Desktop/work/北京/大岳咨询/bid-agent

禁止读取、扫描、修改该目录以外的文件。

服务器项目目录：

/opt/bid-agent

服务器地址：

101.200.154.141

服务器用户：

root

## 2. 开发与运行环境

本地 Mac：
- 负责 Codex、代码编辑、Git 操作
- 当前本地不保证具备完整 Docker、PostgreSQL、Redis 和生产运行环境
- 不得假设所有后端依赖已经在 Mac 本地运行

阿里云 ECS：
- Docker
- Docker Compose
- PostgreSQL
- pgvector
- Redis
- FastAPI
- LangGraph
- 项目正式运行和集成测试环境

服务器当前不保证安装 Codex CLI。
不要要求服务器依赖 Codex CLI 才能运行项目。

## 3. 默认工作流程

Codex 默认在 Mac 项目目录修改代码。

在安全范围内自主完成需求理解、代码阅读、方案设计、实现、测试、问题修复、文档更新和 Git 提交，不因常规、可恢复的开发操作频繁询问用户。

完成修改后：

1. 先检查 git diff
2. 运行本地可执行的静态检查或单元测试
3. 明确说明哪些测试因为本地依赖缺失未运行
4. 进行真实性、数据安全、用户体验和企业交付质量检查
5. 创建可回溯的 Git commit
6. 通过 GitHub 同步到 ECS
7. 在 ECS 的 /opt/bid-agent 下运行 Docker 集成测试

提交信息必须说明修改内容、修改原因和影响范围。重要修改不得只停留在未提交工作区。

不得把“代码写完”视为“功能完成”。

功能完成必须包括：
- 代码实现
- 配置检查
- 数据库迁移检查
- Docker 环境检查
- 接口测试
- 错误处理检查

## 4. 执行前置检查

每次实现新功能前，必须先检查：

- 当前目录
- git status
- 相关现有代码
- pyproject.toml 或依赖文件
- docker-compose.yml
- 环境变量模板
- 数据库模型和迁移
- 现有测试

不要直接凭假设新建重复模块。

## 5. 修改原则

- 先输出实施计划，再修改代码
- 小步修改，优先新增能力并保持兼容
- 发现架构问题时先说明方案，再执行必要的局部重构
- 不进行没有充分理由的大规模重构
- 优先复用现有结构
- 不删除现有功能
- 不修改项目目录之外的配置
- 不读取 SSH 密钥、个人文件或其他项目
- 不把密钥、密码和 Token 写入代码或 Git
- 所有新增环境变量必须同步更新 .env.example
- 所有新增依赖必须写入 pyproject.toml
- 所有数据库结构修改必须提供迁移方案

以下常规操作默认已获用户授权，无需逐步确认：

- 修改项目代码，新增模块和文件
- 优化代码结构，修复 Bug，重构非核心实现
- 编写并运行本地测试、构建和 API 检查
- 修复测试失败，更新项目文档
- 创建 Git commit

以下情况必须先询问用户：

- 改变核心业务流程、产品定位或主要技术路线
- 删除已有核心功能或进行不可逆的大规模重构
- 读取 SSH 私钥、API Key、密码、企业私密数据或项目外个人文件
- 把敏感文件、密钥或企业数据上传到外部服务
- 修改数据库结构、生产服务器、生产数据、生产服务或域名/DNS
- 调用付费 API、创建收费云资源或增加服务器成本
- 清空数据、删除 Docker volume、大量删除文件或修改系统关键配置

禁止执行 `curl ... | sh`、用途不明的 `sudo`、读取 `~/.ssh` 或读取 `.env` 中的敏感内容。发现 Docker、Colima、Lima、虚拟机、数据库或后台服务时，不得直接关闭或删除；先确认其用途和影响。

## 6. 当前产品范围

V1 只重点完成技术方案生成闭环：

上传招标文件
→ 文档解析
→ 提取技术要求与评分点
→ 生成技术方案目录
→ 分章节生成
→ 自动校核
→ 导出 Word

优先级：

1. Requirement Agent
2. Outline Planner
3. Section Writer
4. Reviewer
5. DOCX 导出
6. PGVector / RAG 增强

不要优先扩展与当前闭环无关的功能。

## 7. 生成质量要求

- Requirement 必须可追溯到原文和页码
- 每个章节必须记录响应哪些 Requirement
- 所有生成内容必须可追溯、可验证、可审计
- 不允许虚构企业信息、客户案例、资质、参数、产品能力、法人、成立年份、项目数量或人员履历
- 历史投标文件默认只可作为机构私有 RAG 参考；未经核验不得转化为企业事实
- 不得在用户可见页面或导出文档中暴露数据库 ID、UUID、内部系统字段或 Prompt 标签
- 不一次生成整本方案
- 按章节生成和审核
- 中间结果必须持久化
- 支持失败重试和断点继续
- 自动检查通过不等于完成，必须人工式检查真实性、隐私、可读性和最终交付物

## 8. 测试与故障处理

任何功能修改后优先运行适用的单元测试、集成测试、API 测试和构建测试。

如果测试失败，先自行定位原因、修复并重新验证；不要只报告错误。因本地依赖或权限限制无法执行的测试，应说明限制，并给出在 ECS 或目标环境执行的命令。

## 9. 每次任务的输出格式

开始编码前先输出：

1. 对需求的理解
2. 涉及的已有文件
3. 前置条件
4. 实施步骤
5. 风险
6. 测试方式

编码完成后输出：

1. 修改了哪些文件
2. 完成了什么
3. 尚未完成什么
4. 本地运行了哪些测试
5. 哪些测试必须在 ECS 上执行
6. 部署命令
7. 回滚方式
